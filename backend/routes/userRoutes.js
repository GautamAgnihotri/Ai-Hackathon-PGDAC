// const express = require('express');
// const router = express.Router();
// const userController = require('../controllers/userController');

// // router.post('/', userController.createUser);         // Create new user
// // router.get('/:id', userController.getUser);           // Get user details
// // router.put('/:id', userController.updateUser);        // Update user info
// // router.delete('/:id', userController.deleteUser);     // Delete user
// // router.get('/:id/payment-status', userController.validatePaymentStatus); // Check payment status
// // router.put('/:id/payment-status', userController.updatePaymentStatus);   // Update payment status

// module.exports = router;
